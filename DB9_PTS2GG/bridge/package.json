{
  "name": "db9-gemini-bridge",
  "version": "0.4.7.1",
  "description": "Local bridge between Photoshop UXP plugin and Chrome extension. v0.4.0 adds /presets endpoint for the unified 176-preset library and presetIds pass-through on /generate.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
